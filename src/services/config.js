const apiUrl = '/api/v1'
const greetingUrl = 'https://main.d177anvw131z2v.amplifyapp.com/'
const maxFileSize = 20971520
const acceptedFileImage = "image/jpeg, image/jpg, image/png"
const acceptedFileAudio = "audio/mpeg"

const config = {
    apiUrl,
    greetingUrl,
    maxFileSize,
    acceptedFileImage,
    acceptedFileAudio
}

export default config